/// <reference types="vite/client" />

// Injected by vite.config.ts from package.json, so the version shown in the UI
// can never drift from the one that shipped.
declare const __APP_VERSION__: string;
declare const __BUILD_DATE__: string;
